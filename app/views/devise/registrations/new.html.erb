<div class="row">
 <div class="col-md-4 col-md-offset-4 text-center">
   <div class="well profile-black-white">
      <% if params[:plan] == '2' %>
    <h2>Premium Account</h2>
    <p>Join up for a premium account for £9/month and get access to the group and contact information and their blogs!</p>
  <% else %>
    <h2>Free Account</h2>
    <p>Join for a free account and get basic access to the group</p>
  <% end %>
   </div>
 </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well profile-black-white">
    <h2>Sign up</h2>
      <% if params[:plan] == '2' %>
      <%= render "pro_form" %>
      <% else %>
      <%= render "basic_form" %>
      <% end %>
      
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>